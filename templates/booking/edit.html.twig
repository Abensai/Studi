{% extends 'base.html.twig' %}

{% block title %}{{ 'Edit Booking'|trans }}{% endblock %}

{% block body %}
    <section>
        <div class="container">
            <h1>{{ 'Edit Booking'|trans }}</h1>

            <div class="text-center d-flex">
                <div class="align-self-center">
                    <a href="{{ path('app_booking_index') }}">{{ 'back to list'|trans }}</a>
                </div>

                {{ include('booking/_delete_form.html.twig') }}
            </div>

            {{ include('booking/_form.html.twig', {'button_label': 'Update'}) }}
        </div>
    </section>
{% endblock %}
